#include "blitz/array.h"
#include "common.h"

void load_mask(blitz::Array<bool,3> &maskext) {
  printf("Error: When compiling the python interface to the cloud-identification library the netcdf c++ routines for loading data are not available");
  exit(1);
}

void load_field(blitz::Array<short,3> &fieldext) {
  printf("Error: When compiling the python interface to the cloud-identification library the netcdf c++ routines for loading data are not available");
  exit(1);
}
